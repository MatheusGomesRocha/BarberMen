import React from 'react';
import styled from 'styled-components/native';

const C = styled.View``;
const T = styled.Text``;

export default () => {
    return (
        <C>
            <T> Olá mundo priceTab </T>
        </C>
    );
}